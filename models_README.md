# models folder note

Create a folder named `models/` and place your doctor's model there.

Example:

```text
models/model.joblib
models/scaler.pkl
```

Do not put large model files in GitHub unless your instructor asks for it.
